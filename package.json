{
    "name": "ai-course-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "mkdir -p backend/static && cd frontend && npm install && npm run build && cd .. && cp -r frontend/build/* backend/static/",
        "start": "uvicorn backend.server:app",
        "dev": "concurrently \"cd frontend && npm start\" \"uvicorn backend.server:app --reload\""
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}